{
  "name": "src",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild index.ts --bundle --tsconfig=tsconfig.json --platform=node --outfile=index.js --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tsconfig/node20": "^20.1.4",
    "@types/node": "^22.5.4",
    "@types/selenium-webdriver": "^4.1.26",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "selenium-webdriver": "^4.24.0"
  }
}
